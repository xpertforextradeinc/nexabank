# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Supabase Configurations (Client-side variables require VITE_ prefix)
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="your-public-anon-key"

# Crypto API Keys (Server-side)
COINGECKO_API_KEY=
MOBULA_API_KEY=
COINSTATS_API_KEY=

# Increase API Keys (Server-side)
INCREASE_API_KEY="your-increase-api-key"
INCREASE_API_URL="https://api.increase.com"
INCREASE_WEBHOOK_SECRET="NexaSecureWebhookSecret2026!"

# Supabase Server-side Role Key (Do not expose to client!)
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"
